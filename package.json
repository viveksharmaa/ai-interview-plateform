{
  "name": "ai-interview-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k \"npm --prefix backend run dev --silent\" \"npm --prefix frontend run dev --silent\" ",
    "start": "concurrently -k \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
